import { MarkdownThemeContext } from '../../../theme';
import { ReflectionCategory } from 'typedoc';
export declare function categories(this: MarkdownThemeContext, model: ReflectionCategory[], options: {
    headingLevel: number;
}): string;
