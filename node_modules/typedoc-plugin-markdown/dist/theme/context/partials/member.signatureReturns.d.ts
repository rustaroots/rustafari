import { MarkdownThemeContext } from '../../../theme';
import { SignatureReflection } from 'typedoc';
export declare function signatureReturns(this: MarkdownThemeContext, model: SignatureReflection, options: {
    headingLevel: number;
}): string;
